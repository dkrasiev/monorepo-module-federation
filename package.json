{
  "name": "monorepo-module-federation",
  "version": "1.0.0",
  "scripts": {
    "format": "prettier --write ./packages/**/* ./services/**/*"
  },
  "workspaces": [
    "packages/*",
    "services/*"
  ],
  "devDependencies": {
    "prettier": "^3.2.5"
  }
}
